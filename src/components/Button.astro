---
type Props = {
  text: string;
  colorClass?: string;
  class?: string;
  href?: string;
  icon?: any;
  id?: string;
};

const {
  text,
  colorClass = "bg-vividGreen",
  class: className = "",
  href,
  icon: IconComponent,
  id,
} = Astro.props;

const baseClasses = `${colorClass} hover:bg-green-700 text-white font-bold py-2 px-4 rounded-full uppercase ${className} inline-flex items-center gap-2`;
---

{
  href ? (
    <a href={href} class={baseClasses} id={id}>
      {text}
      {IconComponent && <IconComponent />}
    </a>
  ) : (
    <button id={id} class={baseClasses}>
      {text}
      {IconComponent && <IconComponent />}
    </button>
  )
}
